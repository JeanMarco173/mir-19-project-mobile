import { StyleSheet, Dimensions } from "react-native";

const styles = StyleSheet.create({
  title__text: {
    color: "#276C91",
    fontSize: 24,
    fontWeight: "700",
  },

  help__text: {
    color: "#276C91",
    fontSize: 18,
    fontWeight: "500",
  },
});

export default styles;
